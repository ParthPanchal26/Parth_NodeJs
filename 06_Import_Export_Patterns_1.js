/* This is the 1st pattern to export
  
    const add = (a, b) => {
        return a + b;
    }

    module.exports = add; 
*/

/* This is 2nd pattern to export
    module.exports = (a, b) => {
        return a + b;
    }
*/

/* This is 3rd pattern to export

    const add = (a, b) => {
        return a + b;
    }

    const subtract = (a, b) => {
        return a - b;
    }

    module.exports = {
        add,
        subtract
    }
*/

/* This is 4th pattern to export 
    exports.add = (a, b) => {
        return a + b;
    }

    exports.subtract = (a, b) => {
        return a - b;
    }
*/

