/* JSON 
    JSON -> JavaScript object notation.

*/ 

// const data = require('./08_data.json');
// console.log(data);

console.log("hello parth");

/* Watch Mode 
   -> In the version 18 node introduced watch mode.
   -> Running in watch mode as we call it restarts the process when there is a change in the imported code.
   -> To use watch mode run command node --watch.
   -> With watch mode you don't need to run code again and again from terminal.
*/